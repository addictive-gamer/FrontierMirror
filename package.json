{
  "name": "frontiermirror",
  "version": "1.0.0",
  "private": true,
  "description": "Monitorización UVC profesional con streaming Twitch integrado — single-file PWA",
  "scripts": {
    "check": "node scripts/check.js",
    "build": "node scripts/build.js",
    "deploy:prep": "npm run check && npm run build"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
